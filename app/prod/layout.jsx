export const metadata = {
  title: "Uniik Theo as a Producer",
  description: "Homepage for Uniiktheo as a Producer",
};

export default function ProdLayout({ children }) {
  return <>{children}</>;
}
